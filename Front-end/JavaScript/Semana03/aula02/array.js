let meuArray = [1, 2, 3, 4, 5, 6];

// meuArray.push(7);

// meuArray.pop();

// meuArray.shift();

// meuArray.unshift(0);

// meuArray.concat('c');

// meuArray.join('-');

// meuArray.indexOf(2);

// meuArray.includes(5); // tp um contains

// meuArray.find((item) => item <= 6); // procura o primeiro

// meuArray.every(item => item < 6); // verifica se todos os elementos do array respeitam a funcao

// meuArray.some(item => item > 5); // verifica se existe

// meuArray.reverse();

console.log(meuArray);